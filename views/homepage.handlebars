<ol class="all-posts">
    {{#each posts as |post|}}
    <li>
        {{> post-card post }}
    </li>
    {{/each}}
</ol>

{{!-- Edit Post Form --}}

{{!-- <section class="post_card">
    <div class="post_title_container">
        <div class="post_title">
            Edit Post
        </div>
    </div>
    <div class="post_body">
        <form class="post_form">
            <div class="form_row">
                <label class="display_block" for="newPostTitle">Title</label>
                <input class="post_form_input display_block" type="text" id="newPostTitle"/>
            </div>
            <div class="form_row">
                <label class="display-block" for="newPostContent">Content</label>
                <textarea class="post_form_input display_block" id="newPostContent"></textarea>
            </div>
            <div class="form_row">
                <button type="submit" class="btn form_btn">Update Post</button>
                <button type="submit" class="btn form_btn">Delete Post</button>
            </div>
        </form>
    </div>
</section> --}}